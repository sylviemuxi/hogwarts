# 場景素材庫

固定場景圖放在此資料夾，並由 `scenes.json` 管理。

每個地點建議包含：

- `sceneImage`: 已生成或匯入的圖片路徑
- `scenePrompt`: 用於生成背景圖的固定提示
- `sceneGeneratedAt`: 生成或替換日期

規則：

- 場景圖只畫背景，不包含角色。
- 角色立繪使用獨立圖層疊上去。
- 已鎖定的地點場景不需要每回合重新生成。
- AI 新增地點時，可先建立 `scenePrompt`，等玩家需要時再生成圖片。
